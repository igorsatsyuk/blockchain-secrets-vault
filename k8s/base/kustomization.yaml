apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: blockchain-secrets-vault

resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - blockchain.yaml
  - secrets-api.yaml
  - frontend.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: blockchain-secrets-vault
